# Workflow setup for building Native Android project
workflows:
  android_emulator:
    name: Android Emulator Workflow Simple1
        scripts:
            - ./gradlew assembleDebug
        artifacts:
            - build/outputs/**/**/*.apk